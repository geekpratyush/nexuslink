# SNMP Browser

> **Status:** on the roadmap (Phase 8.6).

The planned SNMP browser (SNMP4J) will support:

- SNMP v1 / v2c / v3.
- A MIB browser, OID walk, and a trap receiver panel.

For now, use the protocol clients listed in the sidebar for application-level connectivity testing.
